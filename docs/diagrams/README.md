# Bento Framework 架构图表

本目录包含 Bento Framework 的各类架构图表，使用 Mermaid 格式绘制。

## 📊 图表索引

### 核心架构图

#### 1. [六边形架构总览](./hexagonal-architecture.md)
展示 Bento Framework 的六边形（端口与适配器）架构全貌，包括驱动侧和被驱动侧适配器。

**何时查看**: 
- 新成员了解整体架构
- 设计新模块时参考
- 向外部解释架构时使用

#### 2. [DDD 分层依赖关系](./layered-dependencies.md)
展示 Core → Domain → Application → Infrastructure → Interfaces 的依赖方向和规则。

**何时查看**:
- 添加新依赖前检查是否违反规则
- import-linter 报错时理解原因
- Code Review 时验证架构合规性

#### 3. [组件关系图](./component-overview.md)
详细展示各层核心组件及其交互关系。

**何时查看**:
- 理解组件职责边界
- 设计新功能时选择正确的层

---

### 流程和序列图

#### 4. [Outbox 模式工作流程](./outbox-pattern-flow.md)
展示事务性消息发送的完整流程，保证数据一致性。

**何时查看**:
- 实现领域事件发布
- 调试事件丢失问题
- 理解分布式事务一致性保证

#### 5. [事件驱动处理流程](./event-driven-flow.md)
从命令触发到事件发布的完整链路。

**何时查看**:
- 设计事件驱动功能
- 排查事件处理问题

#### 6. [用例执行序列图](./usecase-execution-sequence.md)
展示 HTTP 请求 → UseCase → Domain → Repository 的完整调用序列。

**何时查看**:
- 理解请求处理全流程
- 设计新的 UseCase
- 性能优化时分析调用链

---

### 领域建模图

#### 7. [领域模型示例](./domain-model-example.md)
以订单域为例，展示聚合、实体、值对象的建模方式。

**何时查看**:
- 学习如何进行领域建模
- 设计新的聚合根
- 理解 DDD 战术模式

#### 8. [聚合生命周期](./aggregate-lifecycle.md)
展示聚合从创建、修改到持久化的完整生命周期。

**何时查看**:
- 理解聚合的状态管理
- 实现新的聚合根
- 调试领域事件收集问题

---

## 🎨 查看图表的方式

### 方法 1: VS Code 插件预览 ⭐ 推荐
安装 Mermaid Preview 插件后，直接打开 `.md` 文件即可预览：
```bash
# Markdown Preview Mermaid Support
code --install-extension bierner.markdown-mermaid
```

打开任意 `.md` 图表文件，按 `Ctrl+Shift+V` (Windows/Linux) 或 `Cmd+Shift+V` (Mac) 预览。

### 方法 2: GitHub/GitLab 在线预览
直接在仓库中点击 `.md` 文件，GitHub/GitLab 会自动渲染 Mermaid 图表。

### 方法 3: Mermaid Live Editor
访问 [Mermaid Live Editor](https://mermaid.live/)，复制 markdown 文件中的 mermaid 代码块内容即可在线编辑和导出。

### 方法 4: 导出为图片
使用 Mermaid CLI:
```bash
npm install -g @mermaid-js/mermaid-cli

# 从 markdown 文件导出 PNG
mmdc -i hexagonal-architecture.md -o hexagonal-architecture.png

# 导出 SVG
mmdc -i hexagonal-architecture.md -o hexagonal-architecture.svg
```

---

## 📝 编辑图表

图表使用 Markdown + Mermaid 代码块格式，易于版本控制和维护。

**文件格式**:
```markdown
# 图表标题

图表说明文字

\```mermaid
graph TD
    A[开始] --> B{判断}
    B -->|是| C[结果1]
    B -->|否| D[结果2]
\```
```

**官方文档**: https://mermaid.js.org/

---

## 🔄 更新策略

**何时更新图表**:
- ✅ 架构发生重大变更
- ✅ 新增核心模块
- ✅ 依赖关系调整
- ❌ 不需要为每个小的代码改动更新

**负责人**: 架构组 / Tech Lead

**流程**:
1. 修改对应的 `.md` 文件中的 mermaid 代码块
2. 在 Pull Request 中说明图表变更原因
3. Code Review 时一并审查

---

## 📚 相关文档

- [ADR-0001: 六边形架构](../adr/0001-architecture.md)
- [领域建模指南](../conventions/domain-modeling-guide.md)
- [事件设计指南](../conventions/event-design-guide.md)

